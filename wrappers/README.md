# Wrappers

Platform-specific bindings for `encryp-base` will live under this directory.

- `ios/`: SwiftPM package for the Swift wrapper (`wrappers/ios/README.md`)
- `android/`: Android Gradle module scaffold for the JNI / Room wrapper (`wrappers/android/README.md`)
